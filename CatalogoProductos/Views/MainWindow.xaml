<Window x:Class="CatalogoProductos.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CatalogoProductos.ViewModels"
        xmlns:p="clr-namespace:CatalogoProductos.Properties"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" MinHeight="250" MinWidth="600">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <ControlTemplate x:Key="miBoton" TargetType="Button">
            <Border Background="{TemplateBinding Background}"
            BorderThickness="{TemplateBinding BorderThickness}"
            Padding="{TemplateBinding Padding}"
            CornerRadius="5">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="DarkGray" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Background= "#ECECEC">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" x:Name="nombreApp" Text="App" HorizontalAlignment="Center" FontSize="24" Width="44" Margin="5" FontWeight="DemiBold"/>
            <Button Grid.Row="1" x:Name="InicioBoton" Width="65" Height="25" Template="{StaticResource miBoton}" Command="{Binding ActiveHomeViewCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Icons/home.png" Width="15" Height="15" Margin="0,0,2,0"></Image>
                    <TextBlock Text="{x:Static p:Resources.BotonInicio}" />
                </StackPanel>
            </Button>

            <Button Grid.Row="2" x:Name="categoriasBoton" Width="65" Height="25" Template="{StaticResource miBoton}" Command="{Binding ActiveCategoriaViewCommand}" Content="{x:Static p:Resources.BotonCategorias}"/>
            <Button Grid.Row="3" x:Name="productosBoton" Width="65" Height="25" Template="{StaticResource miBoton}" Command="{Binding ActiveProductoViewCommand}" Content="{x:Static p:Resources.BotonProductos}"/>
            <Button Grid.Row="5" x:Name="ajustesBoton" Width="65" Height="25" Template="{StaticResource miBoton}" Command="{Binding ActiveAjustesViewCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Icons/ajustes.png" Width="15" Height="15" Margin="0,0,2,0"></Image>
                    <TextBlock Text="{x:Static p:Resources.BotonAjustes}"></TextBlock>
                </StackPanel>
            </Button>
            <Button Grid.Row="6" x:Name="cerrarSesion" Width="65" Height="25" Template="{StaticResource miBoton}" Command="{Binding CerrarAppCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Icons/exit.png" Width="15" Height="15" Margin="0,0,2,0"></Image>
                    <TextBlock Text="{x:Static p:Resources.BotonSalir}"></TextBlock>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Column="1" Background="#ECECEC">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Grid.Column="0" Focusable="True">
                <Rectangle Height="25" Width="500" Fill="White" Stroke="LightGray" StrokeThickness="2"></Rectangle>
                <TextBox Height="20" Width="470" BorderBrush="Transparent" VerticalContentAlignment="Center" Margin="75,0,100,0" BorderThickness="0,0,0,0" MaxLength="70"/>
                <Image Source="/Icons/buscar.png" Width="20" Height="20" Margin="470,0,0,0"></Image>
            </Grid>

            <Grid Grid.Row="1">
                <Border Background="White">
                    <ContentControl Content="{Binding ActiveView}"/>
                </Border>
            </Grid>

        </Grid>
    </Grid>
</Window>

