<UserControl x:Class="CatalogoProductos.Views.ProductoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CatalogoProductos.ViewModels"
             xmlns:p="clr-namespace:CatalogoProductos.Properties"
             mc:Ignorable="d" 
             PreviewMouseDown="Window_PreviewMouseDown"
             Background="White" d:DesignHeight="400" d:DesignWidth="700">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <ListView x:Name="miListView" SelectedItem="{Binding ProductoSeleccionado, Mode=TwoWay}" ItemsSource="{Binding Productos}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" Width="30" DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Nombre" Width="130" DisplayMemberBinding="{Binding Nombre}"/>
                        <GridViewColumn Header="Descripción" Width="175" DisplayMemberBinding="{Binding Descripcion}"/>
                        <GridViewColumn Header="Precio" Width="50" DisplayMemberBinding="{Binding Precio}"/>
                        <GridViewColumn Header="ID Categoría" Width="50" DisplayMemberBinding="{Binding IdCategoria}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="70*"/>
                <RowDefinition Height="80*"/>
                <RowDefinition Height="110*"/>
                <RowDefinition Height="100*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{x:Static p:Resources.BotonProductos}" FontSize="36" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2.5*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Grid.RowSpan="2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Column="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="25*"/>
                                <RowDefinition Height="50*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="ID" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
                            <TextBox Grid.Row="1" Margin="10,2,10,5" IsEnabled="False" Text="{Binding ProductoSeleccionado.Id, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"></TextBox>
                        </Grid>

                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="25*"/>
                                <RowDefinition Height="50*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Nombre" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
                            <TextBox Grid.Row="1" Margin="10,2,10,5" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"></TextBox>
                        </Grid>

                        <Grid Grid.Column="0" Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="25*"/>
                                <RowDefinition Height="50*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Precio" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
                            <TextBox Grid.Row="1" Margin="10,2,10,5" Text="{Binding Precio, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"></TextBox>
                        </Grid>

                        <Grid Grid.Column="1" Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="25*"/>
                                <RowDefinition Height="50*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="ID Categoría" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
                            <TextBox Grid.Row="1" Margin="10,2,10,5" Text="{Binding IdCategoria, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"></TextBox>
                        </Grid>
                    </Grid>
                </Grid>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25*"/>
                        <RowDefinition Height="125*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Descripción" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
                    <TextBox Grid.Row="1" Margin="0,2,10,5" AcceptsReturn="True" Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"></TextBox>
                </Grid>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Button Grid.ColumnSpan="2" Grid.Row="0" Content="Añadir Categoría" FontSize="18" Margin="10" IsEnabled="{Binding CanAddProducto}" Command="{Binding AddProductoCommand}"></Button>
                <Button Grid.Column="0" Grid.Row="1" Content="Modificar Categoría" Margin="10" IsEnabled="{Binding CanEditDeleteDeselectProducto}" Command="{Binding UpdateProductoCommand}"></Button>
                <Button Grid.Column="1" Grid.Row="1" Content="Eliminar Categoría" Margin="10" IsEnabled="{Binding CanEditDeleteDeselectProducto}" Command="{Binding DeleteProductoCommand}"></Button>

            </Grid>
        </Grid>
    </Grid>
</UserControl>
