<UserControl x:Class="CatalogoProductos.Views.CategoriaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CatalogoProductos.ViewModels"
             xmlns:p="clr-namespace:CatalogoProductos.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="700">

    <UserControl.DataContext>
        <vm:CategoriaViewModel/>
    </UserControl.DataContext>
    
    <Grid Background="White">
        <TextBlock Text="{x:Static p:Resources.TextoCategorias}" FontSize="100" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
